{
    "applicationIdentifier": "{{ .applicationID }}",
    "filterConfig": {
      "pattern": "{{ .address }}",
      "applicationMatchType": "CONTAINS",
      "applicationMatchTarget": "URL"
    },
    "name": "{{ .name }}"
  }